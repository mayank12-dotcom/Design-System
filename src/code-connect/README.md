# Figma Code Connect Notes

This folder tracks the intended Figma-to-code mappings for the EDEN button family.

## Current state

- Figma file key: `0td93qgEm15nkmBt7p2z93`
- Button canvas node: `123:2600`
- Intended mappings live in `mappings.ts`

## Constraint

Figma rejected the automated Code Connect writes because the target components are not published library components yet. Once the button components are published from the EDEN UI Kit library, the mapping list in `mappings.ts` can be replayed into Figma directly.
